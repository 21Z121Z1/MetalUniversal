package com.metallum.client.metal.render;

/** Stable lifetime identity mixed into each Minecraft SingleQuadParticle instance. */
public interface MetalParticleIdentityAccess {
    long metallum$motionIdentity();
}
