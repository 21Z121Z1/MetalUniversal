package com.metallum.client.metal.render;

/** Duck interface used to read an immutable binding name from package-private shader records. */
public interface MetalBindingNameProvider {
    String metallum$bindingName();
}
