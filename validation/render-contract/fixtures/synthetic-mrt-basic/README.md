# Synthetic MRT basic

Two color attachments are written in one logical pass. The fixture records
semantic attachment order and exact CPU-contract expectations. Real Metal 3
and Metal 4 execution is supplied by the `renderContractNativeTest` task.
