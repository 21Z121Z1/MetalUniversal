name: Agent MetalFX piston motion patch

on:
  push:
    branches:
      - agent/metalfx-motion-coverage-26.2

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: ${{ !contains(github.event.head_commit.message, '[piston-motion-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/metalfx-motion-coverage-26.2
          fetch-depth: 0

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'

      - name: Apply source-grounded piston motion patch
        run: python3 .github/scripts/apply_metalfx_piston_motion.py

      - name: Verify Java and piston motion contracts
        run: |
          chmod +x ./gradlew
          ./gradlew compileJava test \
            --tests com.metallum.client.metal.render.MetalPistonMotionTest \
            --tests com.metallum.client.metal.render.MetalFxMotionEligibilityTest \
            --tests com.metallum.client.metal.render.FrameSynthesisContractTest
          git diff --check

      - name: Commit verified patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(metalfx): add exact piston motion vectors [piston-motion-applied]"
          git push origin HEAD:agent/metalfx-motion-coverage-26.2
