name: Agent MetalFX source-history admission fix

on:
  push:
    branches:
      - agent/metalfx-motion-coverage-26.2

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: ${{ !contains(github.event.head_commit.message, '[history-admission-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/metalfx-motion-coverage-26.2
          fetch-depth: 0

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'

      - name: Apply exact-history admission fix
        run: python3 .github/scripts/apply_metalfx_history_admission.py

      - name: Verify Java and frame-history contracts
        run: |
          chmod +x ./gradlew
          ./gradlew compileJava test \
            --tests com.metallum.client.metal.render.MetalFxMotionEligibilityTest \
            --tests com.metallum.client.metal.render.MetalPistonMotionTest \
            --tests com.metallum.client.metal.render.FrameSynthesisContractTest
          git diff --check

      - name: Commit verified patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(metalfx): require exact previous source history [history-admission-applied]"
          git push origin HEAD:agent/metalfx-motion-coverage-26.2
